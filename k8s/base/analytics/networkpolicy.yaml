apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: analytics
  namespace: platform
spec:
  podSelector:
    matchLabels:
      app: analytics
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: platform
          podSelector:
            matchLabels:
              app: api-gateway
      ports:
        - port: 9096
          protocol: TCP
