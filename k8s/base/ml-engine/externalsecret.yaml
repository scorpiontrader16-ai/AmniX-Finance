apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: ml-engine-secrets
  namespace: platform
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: ml-engine-secrets
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: platform/ml-engine
