apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: tenant-operator-secrets
  namespace: platform
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: tenant-operator-secrets
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: platform/tenant-operator
