apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: search-routes
  namespace: platform
  labels:
    app.kubernetes.io/component: api-gateway
spec:
  parentRefs:
    - name: platform-gateway
      namespace: platform
      sectionName: https
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /api/v1/search
      backendRefs:
        - name: search-stable
          port: 9095
      filters:
        - type: ExtensionRef
          extensionRef:
            group: gateway.envoyproxy.io
            kind: SecurityPolicy
            name: jwt-policy-search
